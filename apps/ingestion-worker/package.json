{
  "name": "@rag-bookmarks/ingestion-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@rag-bookmarks/shared": "workspace:*",
    "p-limit": "^7.2.0",
    "p-retry": "^7.1.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240222.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.54.0"
  }
}
